satvos:
  base_url: "https://satvos-backend-production.up.railway.app"
  api_key: "sk_your_api_key_here"

tally:
  host: "localhost"
  port: 0          # 0 = auto-discover
  company: ""      # empty = auto-detect

sync:
  interval_seconds: 30
  batch_size: 50
  retry_attempts: 3

ui:
  port: 8321
